Разработайте RPC-сервер, который умеет вычислять расстояние между двумя точками на плоскости.

В качестве аргумента функция должна принимать тип Points, объявленный следующим образом:
// Точка на плоскости.
type Point struct {
    X, Y float64
}

// Аргумент для функции Dist.
type Points struct {
    A, B Point
}
Сервер должен экспортировать функцию Dist, вычисляющую расстояние между двумя точками 
и записывающую результат вычислений во второй аргумент.